# Environment files (secrets)
.env
.env.*         # .env.development, .env.local, etc.
!.env.example  # Allow .env.example to be tracked

# API keys or credential files
*.json         # Ignores gcp-key.json, ocr-key.json, etc.
app.yaml       # Prevents deployment secrets from being committed

# Python
__pycache__/
*.py[cod]
venv/

# Node (if used)
node_modules/

# Logs
*.log

# macOS system files
.DS_Store
